# Firmware Notes

The Kindle is reported as firmware `5.12.2.2`.

No firmware update or downgrade file was downloaded for this workflow. That is
intentional: the current firmware is already compatible with the selected
WinterBreak2 path, and unnecessary firmware changes increase risk.

Keep the Kindle in Airplane Mode after the jailbreak step unless Wi-Fi is needed
for a documented step.
